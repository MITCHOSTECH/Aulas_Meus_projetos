def seno(n):
    sn = sin(radians(n))
    return sn
def coseno(n):
    cs = cos(radians(n))
    return cs
def tangente(n):
    tn = seno(n) / coseno(n)
    return tn
from math import cos, sin, radians